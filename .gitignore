node_modules
dist
.vite

# Env files — never commit secrets
.env
.env.local
.env.*.local
.env.production

# OS / editor noise
.DS_Store
*.log
.idea/
.vscode/

# Supabase CLI artifacts (if you start using `supabase` CLI)
.supabase/

# Vercel local CLI artifacts
.vercel/
