# Same Supabase project as the web app — copy the values from the
# web .env (VITE_SUPABASE_URL / VITE_SUPABASE_ANON_KEY).
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=

# Deployed web domain — the consent screens link to /privacy and /terms here.
# Use your Vercel domain in production, or http://localhost:5173 for local dev.
EXPO_PUBLIC_WEB_URL=
